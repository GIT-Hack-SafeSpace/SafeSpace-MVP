import React from 'react'

export default function DisplayBio({bio}) {
  // let selectedUser = foundUser(user.userId);
  // console.log(selectedUser,'selected User');
  return (
    <div>
      {bio}
    </div>
  )
}
